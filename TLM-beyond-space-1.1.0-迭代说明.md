# TLM Beyond Space 1.1.0 新迭代基线说明

日期：2026-08-31  
适用环境：Minecraft 1.20.1 / Forge 47.x / Touhou Little Maid 1.5.3  
当前版本：1.1.1（基线版本：1.1.0）
网络协议：9

## 迭代定位

1.1.0 将此前针对救援信号、死亡返程、跨维度转移、未加载女仆和可选模组兼容的连续修改整理为新的开发基线。它不再包含为测试期错误存档状态准备的推测式迁移代码；新逻辑只依据当前实体、明确保存的救援会话和受限持久数据工作。

Promaid、车万女仆：改革和 Maid Useful Task 都是可选软兼容。未安装时不会硬链接其运行类，也不会显示对应 UI。

## 一、最初版本存在的问题

### 1. 救援状态和作息可能无法正常收尾

最初版本在救援开始时会把女仆临时切换到战斗任务、全天作息和非家园状态。如果返程、安全位置检查或其他模组的任务切换打断了结束流程，女仆可能继续保持临时作息或跟随状态。

### 2. 玩家死亡时女仆可能留在死亡点或被再次移动

本模组与 Promaid 同时处理主人死亡。Beyond Space 可能刚把女仆送回救援前位置，Promaid 的主人死亡传送又把她移动到死亡点或重生点。致命伤害信号还可能在主人死亡的短窗口内触发新的普通救援，造成错误原点。

### 3. 返回原点依赖目标区块状态

旧实现会同步读取原点区块，但没有在“寻找安全位置—跨维度转移—提交新实体”的完整事务中持有区块票据。无人区域中还曾使用不适合非活跃区块的实体查询来验证跨维转移，导致成功加入的女仆被误判为失败。

### 4. 水下和危险环境的判定过于笼统

水下没有区分女仆是否装备溺水保护，召唤失败也只能看到“没有安全位置”；岩浆、缺少饰品、碰撞空间不足等原因无法区分。

### 5. 名录信息不足且可能失真

女仆卸载后可能只显示 UUID，缺少最后名称、维度、坐标和救援前原点。TLM 的 `MaidWorldData` 可能因第三方跨维转移留下同一 UUID 的重复离线记录。

### 6. 无法召唤正常卸载的女仆

最初只能处理已经加载的实体，不能根据最后记录受限加载女仆所在区块。第一次加载完成后实体注册稍慢时，还会先提示未找到、短时间内第二次才成功。

### 7. 魂符中的女仆会被误当成失联实体

女仆被收入魂符后，世界实体已经不存在，但旧离线坐标仍可能保留。按该坐标加载区块必然找不到 UUID。

### 8. UI 和失败说明不够清楚

多名女仆连续显示时容易串行；用户只能设置是否召唤，不能区分“仅已加载”和“允许加载”。任务缺少武器、区块加载失败、维度不存在等情况都被归为笼统失败。

### 9. 自动救援、扫把和跟随状态的边界不明确

自动救援可能让骑扫把的女仆下车。普通攻击任务和真正的救援会话在旧状态残留时容易混淆。结束后是否回原点也没有清楚区分原本跟随、家园和坐姿。

### 10. 缺少“车万女仆：改革”的倒地救援入口

安装 MaidReform 后，玩家倒地时没有可选的自动呼救流程；未安装时又必须避免出现无效按钮或硬依赖。

## 二、1.1.0 的正式行为与修改

### 1. 救援会话完整保存并恢复临时状态

- 开始救援前记录原任务、作息、家园模式、坐姿、维度、坐标和救援类型。
- 救援期间临时切换战斗任务、全天作息和所需武器。
- 正常结束、死亡、任务被外部改变或返程转入后台重试时，分别恢复任务、作息、家园模式、坐姿和武器匣。
- 返回安全位置连续失败 100 tick 后，不再无限占用战斗/全天状态；先恢复临时状态，再保存为“待返回”并有限重试。

### 2. 明确的普通自动救援返程规则

- 原本为跟随模式且没有坐下：结束后继续留在主人身边。
- 原本为家园模式，或原本坐下：结束后返回记录位置并恢复原坐姿。
- 原本跟随但距离较远不再作为强制返程条件。
- 普通自动救援不会在主人死亡时启动，也不会利用致命伤害残留创建新会话。

### 3. 手动救援信号和死亡返程

- 手动信号保存召唤前真实维度与坐标；救援结束、主动遣返、退出维度或主人死亡时均尝试返回该原点。
- 玩家死亡与重生时清除致命伤害信号。
- Promaid 安装时，服务器启动阶段只在本次运行内关闭它的跨维跟随与主人死亡传送，避免两个模组同时移动女仆；不会改写 Promaid 配置文件。
- Promaid 不存在或字段变化时兼容逻辑安全跳过，不构成强制依赖。

### 4. 受限区块加载与返回票据

- 用户可为每名女仆选择：关闭、仅已加载、允许加载。
- “允许加载”根据最后记录按 UUID 查找女仆，不从 NBT 重建实体。
- 同时最多处理 2 个区块，每 tick 最多启动 1 个；请求最长 300 tick，区块票据保护最长 440 tick。
- 区块达到 `FULL` 后继续等待实体注册，最多 100 tick（5 秒），解决第一次使用过早报告未找到的问题。
- 返回原点时为安全搜索覆盖的有限区块建立临时 region ticket，直到跨维实体提交完成后释放。返回结束后区块仍可正常卸载。
- 后台待返回队列最长保留 72,000 tick；女仆实体暂未加载时等待，实体恢复后继续尝试。

### 5. 安全位置与明确失败原因

- 候选位置脚下、脚部或头部存在岩浆时禁止召唤。
- 水中位置仅在女仆装备 TLM“溺水保护”饰品时允许。
- 分别显示：岩浆、没有溺水保护、没有碰撞安全空间、维度不存在、区块加载失败、区块中未找到 UUID、未拥有女仆等原因。
- 战斗任务切换区分缺少合适武器、原任务缺失、坐姿解除失败、切换被拒绝和兼容异常。

### 6. 名称、坐标与 MaidWorldData

- 独立 SavedData 按 UUID 保存主人、名称、最后维度、坐标、观察时间和活动救援原点。
- 在线观察最多每 5 秒落盘一次；卸载、维度离开和跨维提交时立即记录。
- UI 同时显示当前/最后联系位置与待返回救援原点，避免把召唤后的当前位置误认为原来的家。
- 读取名录时按 UUID 整理 `MaidWorldData`：只保留最新离线行，实体当前已加载时移除陈旧离线行。该操作不会合并不同 UUID 的真实女仆。

### 7. 魂符识别

- 监听 TLM 女仆与物品互转事件，持久保存收纳状态。
- 对更新前已经存在的魂符，扫描玩家背包、装备、副手和末影箱中的 MaidInfo 补登记。
- 收纳状态优先于旧坐标；名录显示“已收纳于魂符”，不会加载或召唤旧区块。
- 女仆重新放出后自动清除收纳状态。

### 8. 名录 UI

- 每行使用交替背景、左侧状态色条、行号、分隔线和独立操作区域。
- 每名女仆的召唤按钮按“否 → 仅已加载 → 允许加载”循环。
- 坐标、维度、魂符状态和待返回原点使用固定信息行，减少串行误读。
- 旧信号没有 `LoadUnloaded` 字段时仍按“允许加载”读取，保持正式存档兼容。

### 9. 扫把与攻击模式

- 普通自动救援允许骑乘女仆切换到攻击任务并攻击，但不执行主人传送，也不会自动下扫把。
- MaidReform 倒地自动救援同维度时保持骑乘；跨维骑乘无法安全转移时跳过并说明原因。
- 玩家主动使用救援信号时允许女仆下车并传送，不额外提示。
- 单纯处于攻击任务不等于“正在救援”；只有存在本模组保存的活动会话才显示救援状态。

### 10. MaidReform 可选联动

- 仅安装 `maidreform` 时在名录显示“玩家倒地时自动救援”选项。
- 倒地状态从 false 变为 true 时触发一次，从主手、副手和背包中寻找开启该功能的救援信号。
- 每次倒地最多调用 1 名女仆，复用武器、水、岩浆、骑乘和详细失败原因判定。
- `mods.toml` 中为 `mandatory=false`，未安装时不创建按钮，也不预留空白布局。

### 11. 救援持续时间

- 手动救援的最短持续时间为 400 tick（20 秒）。
- `minimumActiveTicks` 和 `maximumActiveTicks` 的配置下限均为 400，避免最大时间小于强制最短时间。
- 默认最大持续时间保持 6000 tick。

## 三、关于“小酒狐和大正女仆酒狐变成跟随”的结论

Promaid 1.0.4 的反编译结果显示：

- `MasterDeathTeleportHandler` 会在主人死亡和重生附近直接传送符合条件的女仆，但不会调用家园模式写入方法。
- `MaidDimensionFollow` 只读取 `isHomeModeEnable()`；只有本来已经是跟随、未坐下的女仆才跨维追随，也不会主动把家园模式改成跟随。
- 当时日志中的“主人……我马上赶到你身边！”确认女仆移动到主人身边是 Promaid 的死亡传送。

因此需要区分两件事：

- 女仆突然出现在主人身边：Promaid 是直接原因。
- 女仆界面中的家园模式真的变成跟随模式：Promaid 代码没有执行该写入。旧 Beyond Space 救援会临时关闭家园模式，跨模组竞态可能打断其恢复，所以不能把模式变化完全归咎于 Promaid。

1.1.0 保留“开始前记录、结束时精确恢复”的正式机制，并在启动时阻止 Promaid 的冲突传送来源。

## 四、从测试迭代中删除的临时代码

- 删除“普通救援原点跨维且女仆已在主人身边，就推测为旧污染并自动清除”的事后修复。该判断只服务于测试期坏状态，可能误伤其他模组的合法移动。
- 删除 Promaid `ownerDeathHandoff` 配置、重生后二次强制校验以及与之相关的等待/强制验证分支。现在直接在启动时关闭冲突处理器，死亡返程只走一个责任明确的流程。
- 删除把 TLM 离线记录无条件覆盖本模组较新最后联系记录的旧污染修复入口；现在只接受时间更新的离线记录。
- 保留旧救援信号缺失字段、正式实体数据和书本绑定的兼容读取。这些属于玩家存档兼容，不是测试期特殊补丁。

## 五、验证与产物

### 1.1.1 行为修正

- 不遣返模式右键遇到正在进行手动救援或等待返回的女仆时，只跳过并说明状态，不再隐式送回原点。
- 只有救援信号处于遣返模式时，普通右键才进入专用遣返流程。
- 9 月 3 日日志显示失败项为“当前未加载”，且没有“正在受限加载”提示，说明部分首次请求没有进入区块等待队列；同一轮又把其他活动女仆送回家并顺带加载附近区块，才造成后续右键能够找到的假象。
- 修正预加载扫描把“最大成功响应数”错误当成“最多检查名录前几名”的问题。现在会按顺序检查至多 20 名已启用女仆，而成功响应数仍受原配置限制；因此前面的女仆已在支援或无法响应时，后面的未加载女仆也能在第一次右键时进入加载队列。
- 对“允许加载但最后坐标尚未进入 SavedData”的女仆新增 100 tick 定位等待；期间持续从当前实体与 TLM 离线记录取得坐标，取得后才排入区块加载。定位最终失败会明确显示“没有可用于加载的最后坐标”，不再笼统显示“当前未加载”。
- 未加载女仆所在区块达到 `FULL` 后，实体解析等待由 100 tick（5 秒）提高到 600 tick（30 秒）；初始缓冲由 2 tick 提高到 10 tick。这是慢速实体注册的兜底，不作为本次即时失败的唯一修复。
- 区块票据上限由 440 tick 提高到 1,200 tick，整批请求寿命由 300 tick 提高到 7,200 tick；并行上限仍为 2，不会无限制加载区块。

- `clean test build`：成功。
- JUnit：34 项，0 失败，0 错误。
- 中英文语言 JSON：UTF-8 严格解析通过。
- 构建文件：`build/libs/tlm_beyond_space-forge-1.1.1.jar`
- SHA-256：`83BBDAE9831AE8C8EAE245092789CEF4001A144FC8D7C8314E1154A1FA9D7E9E`

构建产物不会自动覆盖 `F:\MC` 中正在使用的模组文件。
